interface:
  display_name: "Screen Batch Audit"
  short_description: "Audit built screen batches against map and design doc."
  brand_color: "#0F766E"
  default_prompt: "Use $screen-batch-audit to audit the latest batch of built screens against the surface map and design document."
policy:
  allow_implicit_invocation: true
