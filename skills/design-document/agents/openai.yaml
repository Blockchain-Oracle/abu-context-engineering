interface:
  display_name: "Design Document"
  short_description: "Write the design contract: laws, tokens, per-surface direction."
  brand_color: "#DB2777"
  default_prompt: "Use $design-document to write the design contract (laws, tokens, per-surface direction) for this project."
policy:
  allow_implicit_invocation: true
